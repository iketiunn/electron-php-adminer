// preload.js
// This file is loaded before the renderer process and can expose limited APIs to it

window.addEventListener('DOMContentLoaded', () => {
  // You can add any DOM manipulations or expose APIs here if needed in the future
  console.log('Adminer loaded in Electron')
})